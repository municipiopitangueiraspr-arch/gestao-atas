/* =====================================================================
   INTRANET MUNICIPAL · PREF. PITANGUEIRAS
   intranet-base.css
   ---------------------------------------------------------------------
   Fundação visual compartilhada entre TODOS os módulos da intranet
   (Biblioteca Municipal, Gestão de Atas, Atos Oficiais, Estoque, etc.)
   
   Este arquivo contém:
     · Reset universal e normalização
     · Variáveis globais do design system (cores, sombras, raios)
     · Estrutura de layout (grid sidebar + área principal)
     · Sidebar padronizada
     · Topbar padronizada
     · Área de conteúdo base
     · Loading overlay
     · Utilitários e helpers
     · Responsividade e print
   
   NÃO contém: botões, cards, tabelas, modais, toasts, badges ou
   formulários. Esses ficam em `componentes.css`.
   
   Uso:
     <link rel="stylesheet" href="../shared/css/intranet-base.css">
     <link rel="stylesheet" href="../shared/css/componentes.css">
   ===================================================================== */

/* =====================================================================
   RESET UNIVERSAL
   ===================================================================== */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  -webkit-text-size-adjust: 100%;
  -webkit-tap-highlight-color: transparent;
  scroll-behavior: smooth;
}

body {
  font-family:
    "Segoe UI",
    Roboto,
    system-ui,
    -apple-system,
    sans-serif;
  font-size: 14px;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img,
picture,
video,
canvas,
svg {
  display: block;
  max-width: 100%;
  height: auto;
}

input,
button,
textarea,
select {
  font: inherit;
  color: inherit;
}

button {
  cursor: pointer;
  background: none;
  border: none;
}

a {
  color: inherit;
  text-decoration: none;
}

ul,
ol {
  list-style: none;
}

table {
  border-collapse: collapse;
  border-spacing: 0;
}

h1,
h2,
h3,
h4,
h5,
h6 {
  font-weight: 700;
  line-height: 1.25;
  color: var(--neutral-900);
}

hr {
  border: none;
  border-top: 1px solid var(--neutral-200);
}

/* Remove spinners de inputs number */
input[type="number"]::-webkit-inner-spin-button,
input[type="number"]::-webkit-outer-spin-button {
  -webkit-appearance: none;
  margin: 0;
}
input[type="number"] {
  -moz-appearance: textfield;
}

/* =====================================================================
   VARIÁVEIS GLOBAIS · DESIGN SYSTEM
   ---------------------------------------------------------------------
   Paleta institucional oficial (verde musgo + azul petróleo) idêntica
   à usada na intranet, no módulo Biblioteca e no módulo Atas.
   ===================================================================== */
:root {
  /* ---------- PRIMARY (verde institucional) ---------- */
  --primary-50: #ecfdf5;
  --primary-100: #d1fae5;
  --primary-200: #a7f3d0;
  --primary-300: #6ee7b7;
  --primary-400: #34d399;
  --primary-500: #10b981;
  --primary-600: #0d5e3a; /* ← cor principal institucional */
  --primary-700: #1a3a6b; /* ← cor secundária institucional */
  --primary-800: #064e3b;
  --primary-900: #022c22;

  /* ---------- SUCCESS ---------- */
  --success-50: #ecfdf5;
  --success-100: #d1fae5;
  --success-200: #a7f3d0;
  --success-300: #6ee7b7;
  --success-400: #34d399;
  --success-500: #10b981;
  --success-600: #059669;
  --success-700: #047857;
  --success-800: #065f46;
  --success-900: #064e3b;

  /* ---------- WARNING ---------- */
  --warning-50: #fffbeb;
  --warning-100: #fef3c7;
  --warning-200: #fde68a;
  --warning-300: #fcd34d;
  --warning-400: #fbbf24;
  --warning-500: #f59e0b;
  --warning-600: #d97706;
  --warning-700: #b45309;
  --warning-800: #92400e;
  --warning-900: #78350f;

  /* ---------- ERROR ---------- */
  --error-50: #fef2f2;
  --error-100: #fee2e2;
  --error-200: #fecaca;
  --error-300: #fca5a5;
  --error-400: #f87171;
  --error-500: #ef4444;
  --error-600: #dc2626;
  --error-700: #b91c1c;
  --error-800: #991b1b;
  --error-900: #7f1d1d;

  /* ---------- NEUTRAL (cinzas) ---------- */
  --neutral-50: #f8fafc;
  --neutral-100: #f1f5f9;
  --neutral-200: #e2e8f0;
  --neutral-300: #cbd5e1;
  --neutral-400: #94a3b8;
  --neutral-500: #64748b;
  --neutral-600: #475569;
  --neutral-700: #334155;
  --neutral-800: #1e293b;
  --neutral-900: #0f172a;

  /* ---------- SOMBRAS ---------- */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
  --shadow-lg:
    0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  --shadow-xl:
    0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);

  /* ---------- RAIOS ---------- */
  --border-radius-sm: 0.375rem; /*  6px */
  --border-radius-md: 0.5rem; /*  8px */
  --border-radius-lg: 0.75rem; /* 12px */
  --border-radius-xl: 1rem; /* 16px */
  --border-radius-2xl: 1.125rem; /* 18px ← padrão dos cards */
  --border-radius-full: 9999px;

  /* ---------- TRANSIÇÕES ---------- */
  --transition-base: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-fast: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);

  /* ---------- LAYOUT ---------- */
  --sidebar-width: 260px;
  --topbar-height: 64px;
  --content-max-width: 1500px;

  /* ---------- Z-INDEX (escala semântica) ---------- */
  --z-sidebar: 50;
  --z-topbar: 50;
  --z-dropdown: 100;
  --z-modal: 1000;
  --z-notification: 9999;
  --z-loading: 99998;
  --z-toast: 99999;
}

/* =====================================================================
   BODY · FUNDO PADRÃO DA INTRANET
   ===================================================================== */
body {
  background: linear-gradient(145deg, #e8f5e9 0%, #e2eaf1 100%);
  background-attachment: fixed;
  min-height: 100vh;
  color: var(--neutral-600);
  position: relative;
}

/* =====================================================================
   LAYOUT PRINCIPAL · GRID SIDEBAR + ÁREA PRINCIPAL
   ---------------------------------------------------------------------
   Estrutura usada em todas as páginas dos módulos:
   
   <div class="app-layout">
     <aside class="sidebar">...</aside>
     <div class="main-area">
       <header class="topbar">...</header>
       <main class="conteudo">...</main>
     </div>
   </div>
   ===================================================================== */
.app-layout {
  display: grid;
  grid-template-columns: var(--sidebar-width) 1fr;
  min-height: 100vh;
}

.main-area {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  min-width: 0; /* evita overflow em grid */
}

/* =====================================================================
   SIDEBAR
   ---------------------------------------------------------------------
   Estrutura:
   
   <aside class="sidebar" id="sidebar">
     <div class="sidebar-brand">
       <div class="brand-icon"><i class="fas fa-book"></i></div>
       <div class="brand-text">
         <h2>Nome Módulo</h2>
         <span>Subtítulo</span>
       </div>
     </div>
     <nav class="sidebar-nav">
       <div class="nav-section">Seção</div>
       <a href="..." class="active"><i class="..."></i> Item</a>
       ...
     </nav>
     <div class="sidebar-voltar">
       <a href="..."><i class="fas fa-arrow-left"></i> Voltar</a>
     </div>
   </aside>
   ===================================================================== */
.sidebar {
  background: linear-gradient(180deg, var(--primary-600) 0%, #0a4a2e 100%);
  color: white;
  padding: 1.8rem 1.2rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  position: sticky;
  top: 0;
  height: 100vh;
  overflow-y: auto;
  z-index: var(--z-sidebar);
}

/* ---------- Marca / Brand ---------- */
.sidebar-brand {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0.5rem 0.6rem 1.2rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
}

.sidebar-brand .brand-icon {
  width: 44px;
  height: 44px;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  flex-shrink: 0;
  color: white;
}

.sidebar-brand .brand-text h2 {
  font-size: 1rem;
  font-weight: 700;
  line-height: 1.2;
  color: white;
}

.sidebar-brand .brand-text span {
  font-size: 0.7rem;
  opacity: 0.8;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  color: rgba(255, 255, 255, 0.85);
}

/* ---------- Navegação ---------- */
.sidebar-nav {
  display: flex;
  flex-direction: column;
  gap: 4px;
  flex: 1;
}

.sidebar-nav .nav-section {
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  opacity: 0.55;
  padding: 0.8rem 0.6rem 0.4rem;
  font-weight: 700;
  color: rgba(255, 255, 255, 0.7);
}

.sidebar-nav a {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0.7rem 0.9rem;
  color: white;
  text-decoration: none;
  border-radius: 10px;
  font-size: 0.88rem;
  font-weight: 500;
  transition: var(--transition-base);
  opacity: 0.9;
}

.sidebar-nav a i {
  width: 20px;
  text-align: center;
  font-size: 1rem;
  opacity: 0.85;
}

.sidebar-nav a:hover {
  background: rgba(255, 255, 255, 0.1);
  opacity: 1;
  transform: translateX(2px);
}

.sidebar-nav a.active {
  background: rgba(255, 255, 255, 0.18);
  opacity: 1;
  font-weight: 600;
  border-left: 3px solid #c6f6d5;
}

.sidebar-nav a.active i {
  opacity: 1;
}

/* ---------- Botão voltar ---------- */
.sidebar-voltar {
  padding-top: 1rem;
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}

.sidebar-voltar a {
  display: flex;
  align-items: center;
  gap: 10px;
  color: rgba(255, 255, 255, 0.85);
  text-decoration: none;
  font-size: 0.82rem;
  padding: 0.6rem;
  border-radius: 8px;
  transition: var(--transition-base);
}

.sidebar-voltar a:hover {
  background: rgba(255, 255, 255, 0.1);
  color: white;
}

/* ---------- Scrollbar da sidebar ---------- */
.sidebar::-webkit-scrollbar {
  width: 6px;
}
.sidebar::-webkit-scrollbar-track {
  background: transparent;
}
.sidebar::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 4px;
}
.sidebar::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.3);
}

/* =====================================================================
   TOPBAR
   ---------------------------------------------------------------------
   Estrutura:
   
   <header class="topbar">
     <div class="topbar-esquerda">
       <button class="btn-toggle-sidebar" id="btnToggleSidebar">
         <i class="fas fa-bars"></i>
       </button>
       <div class="topbar-titulo">
         <h1><i class="fas fa-book"></i> Título da Página</h1>
         <p>Subtítulo / descrição</p>
       </div>
     </div>
     <div class="topbar-direita">
       <div class="topbar-info">
         <strong>Nome Usuário</strong>
         <span>Data</span>
       </div>
       <div class="avatar-usuario">AB</div>
       <button class="btn-sair">
         <i class="fas fa-right-from-bracket"></i> Sair
       </button>
     </div>
   </header>
   ===================================================================== */
.topbar {
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--neutral-200);
  padding: 1.1rem 2rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  position: sticky;
  top: 0;
  z-index: var(--z-topbar);
  min-height: var(--topbar-height);
}

/* ---------- Lado esquerdo ---------- */
.topbar-esquerda {
  display: flex;
  align-items: center;
  gap: 12px;
  min-width: 0;
  flex: 1;
}

.topbar-titulo {
  min-width: 0;
}

.topbar-titulo h1 {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--neutral-900);
  display: flex;
  align-items: center;
  gap: 10px;
  line-height: 1.2;
}

.topbar-titulo h1 i {
  color: var(--primary-600);
  flex-shrink: 0;
}

.topbar-titulo p {
  font-size: 0.8rem;
  color: #718096;
  margin-top: 2px;
}

/* ---------- Lado direito ---------- */
.topbar-direita {
  display: flex;
  align-items: center;
  gap: 18px;
  flex-shrink: 0;
}

.topbar-info {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  font-size: 0.78rem;
  color: #718096;
  min-width: 0;
}

.topbar-info strong {
  color: var(--neutral-900);
  font-size: 0.85rem;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 200px;
}

.topbar-info span {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 200px;
}

/* ---------- Avatar ---------- */
.avatar-usuario {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary-600), var(--primary-700));
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 0.95rem;
  border: 2px solid white;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.08);
  flex-shrink: 0;
  text-transform: uppercase;
}

/* ---------- Botão sair ---------- */
.btn-sair {
  background: transparent;
  border: 1px solid var(--neutral-200);
  color: var(--neutral-600);
  padding: 0.5rem 0.9rem;
  border-radius: 8px;
  font-size: 0.82rem;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: var(--transition-base);
  font-family: inherit;
  white-space: nowrap;
}

.btn-sair:hover {
  background: var(--error-50);
  color: var(--error-600);
  border-color: var(--error-600);
}

/* ---------- Botão toggle mobile (só aparece em telas pequenas) ---------- */
.btn-toggle-sidebar {
  display: none;
  background: transparent;
  border: 1px solid var(--neutral-200);
  color: var(--neutral-600);
  width: 40px;
  height: 40px;
  border-radius: 10px;
  cursor: pointer;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  flex-shrink: 0;
  transition: var(--transition-base);
}

.btn-toggle-sidebar:hover {
  background: var(--neutral-100);
}

/* =====================================================================
   ÁREA DE CONTEÚDO
   ---------------------------------------------------------------------
   Uso:
     <main class="conteudo"> ... </main>
   ===================================================================== */
.conteudo {
  padding: 2rem;
  flex: 1;
  max-width: var(--content-max-width);
  width: 100%;
  margin: 0 auto;
}

/* Área de conteúdo ocupando largura total (sem max-width) */
.conteudo.conteudo-full {
  max-width: 100%;
}

/* =====================================================================
   LOADING OVERLAY
   ---------------------------------------------------------------------
   Uso:
     <div class="loading-overlay" id="loadingOverlay">
       <div class="spinner"></div>
     </div>
   
   Ocultar via JS: classList.add('hidden')
   ===================================================================== */
.loading-overlay {
  position: fixed;
  inset: 0;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: var(--z-loading);
  transition: opacity 0.3s ease;
}

.loading-overlay.hidden {
  opacity: 0;
  pointer-events: none;
}

.spinner {
  width: 48px;
  height: 48px;
  border: 4px solid var(--neutral-200);
  border-top-color: var(--primary-600);
  border-radius: 50%;
  animation: intranet-spin 0.8s linear infinite;
}

@keyframes intranet-spin {
  to {
    transform: rotate(360deg);
  }
}

/* Spinner inline (dentro de um container, sem overlay) */
.spinner-inline {
  display: inline-block;
  width: 18px;
  height: 18px;
  border: 2px solid var(--neutral-200);
  border-top-color: var(--primary-600);
  border-radius: 50%;
  animation: intranet-spin 0.8s linear infinite;
  vertical-align: middle;
}

/* =====================================================================
   UTILITÁRIOS
   ===================================================================== */

/* ---------- Acessibilidade ---------- */
.visually-hidden,
.sr-only {
  position: absolute !important;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* ---------- Truncar texto com ellipsis ---------- */
.text-truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* ---------- Display helpers ---------- */
.hidden {
  display: none !important;
}

.d-block {
  display: block;
}
.d-flex {
  display: flex;
}
.d-grid {
  display: grid;
}
.d-inline-flex {
  display: inline-flex;
}

/* ---------- Flex helpers ---------- */
.flex-col {
  flex-direction: column;
}
.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}
.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.flex-1 {
  flex: 1;
}
.gap-1 {
  gap: 0.25rem;
}
.gap-2 {
  gap: 0.5rem;
}
.gap-3 {
  gap: 1rem;
}
.gap-4 {
  gap: 1.5rem;
}

/* ---------- Texto ---------- */
.text-center {
  text-align: center;
}
.text-right {
  text-align: right;
}
.text-left {
  text-align: left;
}

.text-sm {
  font-size: 0.8rem;
}
.text-xs {
  font-size: 0.7rem;
}
.text-lg {
  font-size: 1.1rem;
}

.text-muted {
  color: var(--neutral-400);
}
.text-success {
  color: var(--success-600);
}
.text-warning {
  color: var(--warning-600);
}
.text-error {
  color: var(--error-600);
}
.text-primary {
  color: var(--primary-600);
}
.text-bold {
  font-weight: 700;
}
.text-semibold {
  font-weight: 600;
}

/* ---------- Margens utilitárias ---------- */
.mt-1 {
  margin-top: 0.25rem;
}
.mt-2 {
  margin-top: 0.5rem;
}
.mt-3 {
  margin-top: 1rem;
}
.mt-4 {
  margin-top: 1.5rem;
}
.mt-5 {
  margin-top: 2rem;
}

.mb-1 {
  margin-bottom: 0.25rem;
}
.mb-2 {
  margin-bottom: 0.5rem;
}
.mb-3 {
  margin-bottom: 1rem;
}
.mb-4 {
  margin-bottom: 1.5rem;
}
.mb-5 {
  margin-bottom: 2rem;
}

.ml-auto {
  margin-left: auto;
}
.mr-auto {
  margin-right: auto;
}
.mx-auto {
  margin-left: auto;
  margin-right: auto;
}

/* ---------- Largura ---------- */
.w-100 {
  width: 100%;
}
.w-auto {
  width: auto;
}

/* =====================================================================
   RESPONSIVIDADE
   ===================================================================== */

/* ---------- Tablets grandes e telas médias ---------- */
@media (max-width: 1024px) {
  /* Sidebar vira off-canvas: sai da tela e entra ao clicar no hamburger */
  .app-layout {
    grid-template-columns: 1fr;
  }

  .sidebar {
    position: fixed;
    left: -280px;
    width: var(--sidebar-width);
    z-index: 1000;
    transition: left 0.3s ease;
    box-shadow: var(--shadow-xl);
  }

  .sidebar.aberta {
    left: 0;
  }

  /* Botão hamburger aparece */
  .btn-toggle-sidebar {
    display: inline-flex;
  }

  /* Backdrop quando sidebar está aberta (opcional, injetado via JS) */
  .sidebar-backdrop {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    z-index: 999;
    animation: fadeIn 0.2s ease;
  }

  .sidebar-backdrop.aberta {
    display: block;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
}

/* ---------- Tablets pequenos e celulares grandes ---------- */
@media (max-width: 768px) {
  .conteudo {
    padding: 1.2rem;
  }

  .topbar {
    padding: 0.9rem 1.2rem;
  }

  .topbar-titulo h1 {
    font-size: 1.1rem;
  }

  .topbar-titulo p {
    display: none;
  }

  .topbar-info {
    display: none;
  }

  .topbar-direita {
    gap: 10px;
  }

  .btn-sair span,
  .btn-sair .btn-sair-texto {
    display: none;
  }

  .btn-sair {
    padding: 0.5rem;
    width: 40px;
    height: 40px;
    justify-content: center;
  }
}

/* ---------- Celulares pequenos ---------- */
@media (max-width: 480px) {
  .conteudo {
    padding: 1rem;
  }

  .topbar {
    padding: 0.75rem 1rem;
    gap: 8px;
  }

  .topbar-titulo h1 {
    font-size: 1rem;
    gap: 6px;
  }

  .avatar-usuario {
    width: 36px;
    height: 36px;
    font-size: 0.85rem;
  }

  .sidebar-brand .brand-icon {
    width: 40px;
    height: 40px;
    font-size: 1.2rem;
  }

  .sidebar {
    padding: 1.4rem 1rem;
  }

  .sidebar-nav a {
    padding: 0.65rem 0.75rem;
    font-size: 0.85rem;
  }
}

/* =====================================================================
   IMPRESSÃO
   ---------------------------------------------------------------------
   Esconde a sidebar, a topbar, o botão voltar e o loading overlay.
   As páginas específicas podem complementar com @media print próprio.
   ===================================================================== */
@media print {
  body {
    background: white !important;
    padding: 0;
    font-size: 11pt;
  }

  .sidebar,
  .topbar,
  .btn-toggle-sidebar,
  .sidebar-backdrop,
  .loading-overlay,
  .notification-center {
    display: none !important;
  }

  .app-layout {
    grid-template-columns: 1fr !important;
    min-height: auto;
  }

  .main-area {
    min-height: auto;
  }

  .conteudo {
    padding: 0 !important;
    max-width: 100% !important;
    margin: 0 !important;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  /* Evita quebra dentro de blocos críticos */
  table,
  .card,
  .secao {
    page-break-inside: avoid;
  }
}

/* =====================================================================
   ALTO CONTRASTE (opcional)
   ---------------------------------------------------------------------
   Ativado via JS: document.body.classList.add('alto-contraste')
   ===================================================================== */
body.alto-contraste {
  --primary-600: #003d20;
  --primary-700: #0a1f3a;
  --neutral-600: #1e293b;
  --neutral-700: #0f172a;
}

body.alto-contraste .sidebar {
  background: #000000;
}

body.alto-contraste .topbar {
  background: #ffffff;
  border-bottom-color: #000000;
}

body.alto-contraste .topbar-titulo h1 {
  color: #000000;
}

body.alto-contraste a:focus,
body.alto-contraste button:focus {
  outline: 3px solid #ffcc00;
  outline-offset: 2px;
}
